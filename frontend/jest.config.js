export default {
    testEnvironment: 'jsdom',
    transform: {},
    moduleFileExtensions: ['js', 'json', 'node'],
    verbose: true,
    roots: ['<rootDir>/src'],
    testMatch: ['**/*.test.js'],
};
